import styled from 'styled-components';

const Form = styled.form`
  text-align: center;
  margin-bottom: 30px;
  display: flex;
  justify-content: center;
`;

const Input = styled.input`
  width: 240px;
  height: 35px;
`;

const Btn = styled.button`
  align-items: center;
  width: 40px;
`;

export { Form, Input, Btn };
